<nav class="navbar navbar-expand-lg " style="background-color:#2ecc71;">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">🍋 Lemon fitness</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        {# <li class="nav-item">
          <a class="nav-link" href="{{path('app_partner_profile', {id: partner.id})}}">mon compte</a>
        </li> #}
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{path('app_logout')}}">Se deconnecter</a>
          </li>
          {% if is_granted('ROLE_PARTNER') %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('app_partner_profile', {id: partner.id })}}">mon compte</a>
            </li>
          {% elseif is_granted('ROLE_STRUCTURE') %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('app_structure_profile', {id: structure.id })}}">mon compte</a>
            </li>
          {% endif %}
          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('app_register_partner')}}">Ajouter un partenaire</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('app_register_structure')}}">Ajouter une salle</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('app_partner_list', {page: 1, nbre: 4})}}">Liste des partenaires</a>
            </li>  
          {% endif %}         
        {% else %}  
          <li class="nav-item">
            <a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
          </li>
        {% endif %}        
      </ul>
    </div>
  </div>
</nav>